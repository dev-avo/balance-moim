# Cloudflare Pages _redirects 파일
# 포맷: [source] [destination] [code?]
# 지원되는 상태 코드: 301, 302 (기본값), 303, 307, 308
# 200 코드는 rewrite를 의미하며 SPA 라우팅에 사용됩니다

# 중요: Functions가 우선순위가 높지만, API 경로를 명시적으로 제외하여 안전하게 처리
# _redirects는 Functions가 처리하지 않는 경로에만 적용됩니다

# SPA 라우팅: API 경로를 제외한 모든 경로를 index.html로 rewrite
# API 경로(/api/*)는 Functions가 처리하므로 여기서 제외됨
/*    /index.html   200

